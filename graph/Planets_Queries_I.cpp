#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;
    vector<int> planet(n + 1);
    for (int i = 1; i <= n; i++)
    {
        cin >> planet[i];
    }
    
}